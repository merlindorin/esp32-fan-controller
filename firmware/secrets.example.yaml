# Copy this file to secrets.yaml and fill in the real values.
# secrets.yaml is git-ignored.
wifi_ssid: "your-wifi-ssid"
wifi_password: "your-wifi-password"
fallback_password: "set-a-strong-password"
api_encryption_key: "generate-with: openssl rand -base64 32"
ota_password: "generate-with: openssl rand -hex 16"
